name: theorem-complex-ci

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  core:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -r requirements-dev.txt
      - run: python -m compileall -q equations empirical theoretical_benchmarks the_nothingness_effect.py
      - name: Core tests without optional AI backend
        run: >-
          python -m pytest -q
          --ignore=tests/numerical
          --ignore=tests/contracts/test_qenn_contracts.py
          --ignore=tests/contracts/test_pgqenn_contracts.py
          --ignore=tests/contracts/test_soinets_contracts.py
          --ignore=tests/artifacts/test_qenn_artifacts.py
          --ignore=tests/artifacts/test_pgqenn_artifacts.py
          --ignore=tests/artifacts/test_soinets_artifacts.py

  theorem-contracts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install -r requirements-dev.txt -r requirements-ai.txt
      - run: python -m pytest -q tests/contracts tests/invariants tests/boundaries tests/source_removal

  artifact-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install -r requirements-dev.txt -r requirements-ai.txt
      - run: python -m pytest -q tests/artifacts

  ai-cpu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install -r requirements-dev.txt -r requirements-ai.txt
      - run: python -m pytest -q tests/numerical/test_qenn_model.py tests/numerical/test_pgqenn_model.py tests/numerical/test_soinets_model.py

  security-and-provenance-guards:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install -r requirements-dev.txt -r requirements-ai.txt
      - run: python tools/check_no_authoritative_appendices.py
      - run: python -m tools.qa_guards
