# TNE AI derivation integration report

QENN, PGQENN, and SOInets each execute all six requested output groups in their own `test/artifacts` and `simulation/artifacts` trees: color classification, sound classification, bidirectional color classification, bidirectional sound classification, color cloning, and sound cloning.

The cross-appendix AI matrix contains 280 verified mappings: 22 `blocked_source_law`, 10 `integrated_runtime`, 11 `integrated_shared_primitive`, 128 `planned_proxy_source`, and 109 `typed_contract_available`.

Every row now records its source theorem's current implementation status. No row carries the retired carrier-conflict classification. Typed availability does not by itself claim that every target architecture consumes the source; target-specific wiring remains explicit in the evidence column.

Canonical AI execution is CPU-testable, fail-closed for non-finite values, and uses deterministic seeds. PGQENN random sampling remains only a named ablation/comparison mode.

## Executable dependency chain

The canonical runtime now enforces this concrete chain:

1. `DTQC -> QENN`: a Fibonacci/Pisot inflation layer performs finite spectral-support projection, elastic support transport, Parseval checks, and reconstruction before QENN memory/readout.
2. `QENN + MPL-TC -> PGQENN`: PGQENN consumes the QENN hidden field and the prime/motif prefix returned by the pinned MPL-TC implementation. A local prime list is not accepted as a substitute for canonical motif scheduling.
3. `QENN + PGQENN -> SOInets`: SOInets aggregate multiple instances of both architectures and retain the upstream dependency provenance.

QENN also directly composes Flowpoint involution, invariant/anti-invariant projectors, normalized DFI, parity-conditioned pDFI, exact unclipped Elastic-π, the Elastic-π weighted transition norm, an exact Elastic Dubler feature-window bridge, typed observation/collapse, spectral reconstruction, Parseval, and an aggregate completeness residual. Observation/collapse produces a normalized differentiable probability field plus a definite idempotent outcome projection. Source-removal modes for DTQC, Elastic Dubler, and observation/collapse are explicit ablations rather than alternate canonical implementations.

The MPL-TC dependency is pinned at commit `056e346824e9ec9785ab45b642b3b842c88f6e56`. Its `mccrackns_prime_law.py` module must match SHA-256 `016e7476606ba4c364dc5daa8ac1bcd77a418b1d5e244137559609e157f04dc9`; otherwise PGQENN fails closed. See `docs/data/mpl_tc_dependency_manifest.json`.

The seven implemented DTQC contracts plus the exact Elastic Dubler duality and two Elastic-π norm source laws executed by QENN are marked `integrated_runtime` in the machine-readable integration matrix. Other rows retain their prior status; these adapters do not imply blanket theorem coverage.

## Multimodal SOInet

The canonical multimodal wrapper accepts named finite tensor modalities, applies observation/collapse before a single shared encoder, evaluates exact modality-level Elastic Dubler ratios, fuses the modality tokens, and runs the collapsed/fused carriers through SOInets. It therefore inherits the complete DTQC→QENN and QENN+MPL-TC→PGQENN chain. A shared decoder reconstructs finite modality tokens; it does not claim full-resolution world-model generation.

The uploaded multimodal project was used only as external design context. Its archive SHA-256 is `EE6A04D89EBE5DA78D4F93950789A8833A904FF29A8F03DECDC97C621199ADB4`; no source or artifact from it was copied. The repository implementation replaces its clipped Elastic-π and minimal QENN/PGQENN proxies with current typed source laws. Details and regeneration commands are in `docs/tne_multimodal_architecture.md`.

The trainable product is exposed directly as
`artificial_intelligence.multimodal.TNETrainableMultimodalModel`. It adds a
per-sample task readout and shared token decoder over the SOInet backbone so
training, validation, calibration, confusion, latent geometry, reconstruction,
gradient, residual, and source-removal evidence are all executable rather than
represented by one batch-aggregate demonstration.
