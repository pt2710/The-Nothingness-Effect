# TNE AI theorem-complex usage assessment

The external outline `TNE_QENN_PGQENN_SOInets_Theorem_Complex_Outline.md`
(SHA-256 `93D4BE6A5EFDE381FC813CA10674500CC864C41D0F5E05779A531E94CC59E2CE`)
contains 137 named upstream/native candidates. It was used as a targeting index;
it was not copied into the repository and does not replace the seven external
authoritative appendices.

The row-level assessment is in
`docs/data/ai_derivation_integration_matrix.csv`. Its 280 appendix-verified AI
mappings cover the outline and the relevant derived B/C compositions. Every
row records QENN, PGQENN and SOInet uses plus four explicit decisions:
`use_decision`, `optimization_hypothesis`, `optimization_metric`, and
`optimization_conclusion`.

## Assessment result

| Decision | Rows | Meaning |
| --- | ---: | --- |
| `use_now_with_ablation` | 23 | Runtime/shared primitive exists; structural effect is tested, while task benefit remains dataset-specific. |
| `evaluate_typed_candidate` | 107 | Typed theorem contract exists; a target-specific bridge and controlled ablation are still required. |
| `defer_until_source_complete` | 128 | Related proxy code is not sufficient for canonical enablement or an optimization claim. |
| `blocked_until_source_law` | 22 | The upstream A source law must be completed before an admissible AI experiment. |

No theorem complex is enabled merely because its name sounds relevant. A
candidate is useful only if the target architecture declares its input/output
map, preserves the source invariant, exposes a failure residual, and shows a
non-zero source-removal effect. “Optimization” means improvement of a
predeclared held-out metric without worsening its closure/failure boundary; a
finite result alone is not improvement.

## Group conclusions

| Outline group | Current use | Optimization question and boundary |
| --- | --- | --- |
| Flowpoint and closure | Active in Q/P/S projectors, reversible sign channels, C2-equivariant layers and memory transfer. | Test involution/equivariance and reverse-reconstruction residuals. More elaborate schedules, groupoids and compensation transports remain typed candidates or proxy gaps. |
| Flowpoint spatiality | Partly active in PGQENN locality and SOInet meta-adjacency. | Test localization, boundary leakage and spectral reconstruction; do not infer spatial closure from an adjacency matrix alone. |
| DFI | Canonical normalized DFI is active and fail-closed. | Test spiking/drift and validation objective. SOI rescaling must leave normalized DFI invariant. |
| pDFI | Active as parity-conditioned transition diagnostics. | Test parity leakage and source removal. Meta-collapse and full pDFI synthesis require their complete interfaces. |
| Elastic-pi | Exact unclipped source law is active in Q/P/S. | Tune positive `K_D` against validation, calibration and residual metrics; clipping never counts as exact semantics. |
| Elastic-pi norm | Active as a weighted finite transition diagnostic. | Test transition cost, Parseval/reconstruction and norm residuals before using it as pruning or optimizer control. |
| Elastic Dubler | Exact ratio/log-shift is active for QENN feature windows and named multimodal domains. | Test cocycle/normalization residuals and held-out source-removal change; it is not automatically a learning-rate law. |
| DTQC | Active before QENN spectral memory and inherited by PGQENN/SOInets. | Test support leakage, resonance, Parseval and reconstruction. A pure-point numerical candidate is not a proof. |
| Spectrum of Infinities | SOI normalization gain is active; full infinity-shell realization is incomplete. | Tune the positive normalization carrier while preserving DFI similarity. Shell/address/measurability claims remain bounded by implemented contracts. |
| Observation/collapse | Active at raw-modality, QENN, PGQENN and SOInet readouts. | Test calibration, distinguishability, idempotence and ambiguity; internal state difference is not automatically observable. |
| MPL-TC | Pinned positive prime/motif dependency is active in PGQENN and inherited by SOInets. | Four verified positive TC streams affect messages. Negative nodes are explicitly a TNE Flowpoint lift, not an MPL-TC claim. |
| Native QENN | Runtime uses the named shared sources; only source-complete registered chains are promoted. | Use the row metrics for spectral, drift, parity, optimizer and curvature ablations; remaining native rows keep their typed/proxy status. |
| Native PGQENN | Prime growth, four TC streams, signed lift, pDFI, Elastic-pi and QENN/DTQC inheritance are active. | Measure motif coverage, signed-spectrum removal, shell stability, Parseval and SOI normalization. |
| Native SOInets | Multiple QENN/PGQENN subnetworks, bidirectional memory and meta-collapse are active. | Measure cross-modal transfer, cloning, calibration and meta/spatial residuals; universal claims remain unpromoted. |

## Newly executable optimization probes

- The multimodal trainer performs deterministic coordinate search over both
  exact positive `K_D` and the SOI normalization gain `gamma`.
- `gamma` acts on the appendix carrier `W_tilde = gamma^-1 W`; it does not
  alter the scale-invariant DFI law. The DFI rescaling residual is recorded.
- PGQENN grows the four positive MPL-TC streams and simultaneous paired
  negative TNE nodes in 3D. Positive and negative geometry need not be mirror
  symmetric, but the value partner map is an exact involution.
- Producer-local CSV, PNG and GIF evidence records joint hyperparameter probes,
  signed-spectrum topology, asymmetric adjacency, signal propagation and
  source-removal comparisons.

## Observed deterministic probe results

| Run | Epochs / probes | `K_D` start -> end | `gamma` start -> end | Validation objective start -> end | Sum of accepted local-search improvement |
| --- | ---: | --- | --- | --- | ---: |
| test producer | 12 / 72 | 0.7200 -> 2.8329 | 1.2500 -> 1.5625 | 1.4595 -> 0.8168 | 0.01682 |
| simulation producer | 40 / 220 | 0.7200 -> 2.8800 | 1.2500 -> 2.0480 | 1.4595 -> 0.1740 | 0.03075 |

Every accepted coordinate-search step is compared with the unchanged local
state at that epoch, so the recorded local improvement is attributable to the
hyperparameter proposal. The larger end-to-end objective reduction also
contains ordinary training progress and is therefore not attributed solely to
`K_D` or `gamma`.

Removing the signed negative-spectrum channel changed held-out cross entropy by
`1.39e-5` in the short producer and `4.28e-6` in the longer producer. This is a
non-zero dependency witness, but not evidence that the channel improves this
small synthetic task. Its present benefit is structural expressivity and a
falsifiable signed-growth mechanism; task-performance benefit remains an
empirical question for a real multimodal dataset.

These are deterministic synthetic QA experiments. They establish executable
effects and falsifiable optimization hypotheses, not empirical generalization
or formal theorem proof.
