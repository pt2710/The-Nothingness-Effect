# TNE artifact provenance report

The aggregate manifest covers all **351** implemented theorem contracts with no missing or duplicate ID. It indexes **441** tables, **353** static figures, **147** tracked GIF animations, **20** audio files, and **33** animation generators. All **351** theorem rows have a complete JSON/CSV/NPZ/PNG core bundle.

Result files live beneath the producing module's `test/artifacts` or `simulation/artifacts` directory. Producer-local evidence contains **348** manifests. QENN, PGQENN, SOInets, and multimodal outputs record architecture-specific 3D growth, matrix, source-stream, signed-state and signal-propagation evidence. Large regenerable frame dumps and videos remain external. Every theorem manifest records checksum, start/result commit, seed, tolerances, residual vector, closure status, generated files, regeneration command, active-catalog evaluation metadata, and the finite-evidence claim boundary.
