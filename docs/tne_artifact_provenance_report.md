# TNE artifact provenance report

The aggregate manifest covers all **173** implemented theorem contracts with no missing or duplicate ID. It indexes **277** tables, **333** static figures, **137** tracked GIF animations, **20** audio files, and **33** animation generators.

Result files live beneath the producing module's `test/artifacts` or `simulation/artifacts` directory. Producer-local evidence contains **202** manifests. Each multimodal mode owns fourteen tables, thirty-two figures, nineteen compact GIFs, and three manifests. QENN, PGQENN, SOInets, and multimodal each record architecture-specific 3D growth, matrix, source-stream, signed-state and signal-propagation evidence. Large regenerable frame dumps and videos remain external. Every theorem manifest records checksum, start/result commit, seed, tolerances, residual vector, closure status, generated files, regeneration command, and the finite-evidence claim boundary.
