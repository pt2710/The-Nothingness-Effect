# TNE artifact provenance report

The aggregate manifest covers all **173** implemented theorem contracts with no missing or duplicate ID. A clean external regeneration indexes **229** tables and **221** static figures. Producer-local evidence includes **186** manifests, **49** tracked GIF animations, **20** audio files, and **31** animation generators.

Result files live beneath the producing module's `test/artifacts` or `simulation/artifacts` directory. The standalone trainable multimodal test and simulation pipelines contribute 14 tables, 24 figures, 10 compact GIFs, and two manifests to those producer-local counts. Large regenerable frame dumps and videos remain external. Every theorem manifest records checksum, start/result commit, seed, tolerances, residual vector, closure status, generated files, regeneration command, and the finite-evidence claim boundary.
