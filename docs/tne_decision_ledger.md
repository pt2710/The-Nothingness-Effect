# TNE consistency revision decision ledger

| ID | Decision | Evidence and consequence |
| --- | --- | --- |
| D-001 | Keep all nine uploaded appendix LaTeX files outside every repository Git root; only the specified seven participate in the 351-complex inventory. | External sources are read only. Repository outputs contain filenames, theorem IDs, labels, checksums, and bounded status metadata only. |
| D-002 | Use a Python-identifier-safe local checkout directory. | A hyphenated clone directory caused 162 setup errors before any source edit; the same commit passed 162 tests after relocation. Root import handling is also made checkout-name independent for CI. |
| D-003 | Disambiguate the audit's four duplicate slug IDs with module prefixes. | The audit has 351 rows but only 347 unique slugs. Both colliding records receive `module::source_slug`; the original audit value remains in `source_complex_id`. |
| D-004 | Treat finite residual reduction as evidence, never as proof by itself. | Runtime statuses distinguish `numerical_candidate`, `closed`, `open`, `blocked`, and `singular`; clipping is approximation metadata. |
| D-005 | Keep legacy/proxy coverage visible until every contract gate is present. | Existing related code maps to `proxy_only`; a file's existence alone does not upgrade a theorem-complex status. |
| D-006 | Implement Flowpoint from the canonical linear involution, not the historical XOR presentation. | `F(x)=-x`, the exact parity schedule, balance-fiber transport, and affine history field are typed source laws. `fp` remains a compatibility generator, including Boolean toggling. |
| D-007 | Represent a computed 2-adic value as an exact finite prefix with an explicit digit count. | A finite integer is not presented as a completed infinite 2-adic object. |
| D-008 | Implement the foundational closure checkpoint first through the six complete duality contracts and keep unrelated closure families at proxy status. | This avoids inventing formulas for the remaining symmetry, spatiality, infinity, SOI, and observation complexes while establishing reusable involution/action/field primitives. |
| D-009 | Keep finite calibrated mathematical fields at `numerical_candidate`. | Residual and reconstruction success do not establish a mathematical minimizer or attainment theorem. |
| D-010 | Make canonical DFI fail closed and confine its historical neutral NaN/Inf coercion to explicit compatibility mode. | A zero remainder denominator now returns a typed singular witness; canonical arrays remain unavailable rather than becoming zero or one. |
| D-011 | Keep exact Elastic-π semantics separate from floating-point approximation policy. | The source law is `pi * exp(-S/K_D)` with `K_D > 0`. Underflow/overflow has an explicit status; exponent clipping requires diagnostic return metadata and cannot use the legacy tuple API. |
| D-012 | Type integer parity labels separately from operator parity and reject zero pDFI predecessors. | Orbit pDFI uses `x mod 2` only for integer trajectories; the inverse response recurrence is a separately declared positive two-cycle and is not inferred from commutation alone. |
| D-013 | Preserve the Elastic-π weighted path domain distinction. | The difference functional is a norm on the declared anchored domain and a seminorm on unrestricted sequences. pDFI comparison requires nonzero predecessors, common input, fixed calibration, and a zero residual; equality is not automatic. |
| D-014 | Make the central facade argument-correct and typed while retaining named compatibility entry points. | `fp`, symmetry, duality, spatiality, and countable infinity now forward required arguments. `dfi` returns the canonical typed result; historical dictionary output is isolated as `legacy_dfi` with a deprecation warning. |
| D-015 | Promote only bounded, source-faithful physical chains rather than treating every legacy simulation as a theorem implementation. | Seven physical modules each implement a selected 4A→2B→1C chain. Existing toy/proxy backends remain evidence only for every other matrix row. Each C result is a numerical candidate with boundary, localization, coercivity, observability, and source-removal diagnostics. |
| D-016 | Keep representational completeness, theoremhood, and formal proof as distinct statuses. | The existing finite dual-closure simulator is reused as bounded evidence. Gödel-like boundaries, finite 2-adic prefixes, manifestability gates, idempotent splittings, transport, Noether transgression, sheaf gluing, and terminal quotients expose residuals and obstructions; no finite fixed point is promoted to a proof. |
| D-017 | Use PyTorch as an optional CPU-testable AI backend while keeping the core dependency set unchanged. | `requirements-ai.txt` pins the supported 2.13 minor line; canonical QENN layers are differentiable, device-neutral, and guarded against non-finite inputs. GPU and graph frameworks are not required. |
| D-018 | Promote only the first source-complete QENN 4A→2B→1C chain. | The two B laws implement the appendix's positive residual-energy coupling, not product carriers. The C law is one boundary-sensitive defect field with both-B ablations and remains a numerical candidate/open result. The other QENN rows remain proxy-only. |
| D-019 | Replace random sampling as the canonical PGQENN growth law with deterministic prime-gap, parity-phase, and typed 2-adic-depth growth. | The historical `random.sample` behavior is retained only in `stochastic_comparison_graph` as a seeded ablation. Canonical message passing is Flowpoint/C2 equivariant and uses pDFI and exact unclipped Elastic-π gains. |
| D-020 | Promote only the first source-complete PGQENN 4A→2B→1C chain. | Both B laws are positive residual-energy couplings with all-source ablations. The C law is one graph-local field with boundary, leakage/localization, coercivity, observability, and numerical-candidate status; other PGQENN rows remain proxy-only. |
| D-021 | Bind SOInet B19 to A01+A13 and B20 to A02+A14, following the actual appendix composition rather than a preliminary A01–A04 working selection. | The authoritative source lists under `app:soinet_complex_19` and `app:soinet_complex_20` name those dependencies explicitly. This prevents a formally typed but mathematically wrong B contract. |
| D-022 | Keep `tne_concepts/SOInet/soi_net.py` as a legacy experimental backend and expose the canonical implementation through a deprecating compatibility import. | The legacy module owns duplicated Flowpoint/DFI/Elastic-π code and random graph growth. New SOInet code composes the shared QENN/PGQENN primitives; no legacy duplicate is imported into the canonical path. |
| D-023 | Promote only the source-complete SOInet A01+A13/A02+A14→B19+B20→C29 chain. | The meta-network runs multiple QENN and PGQENN subnetworks, bidirectional memory, aggregated collapse readout, spectral/spatial residuals, and completeness-based fail-closed arbitration. C29 remains a numerical candidate. |
| D-024 | Keep large regenerated QA artifacts outside Git and track one aggregate manifest plus three representative AI figures. | Eighteen suites generated 136 unique theorem manifests, 18 tables, and 18 figures at seed 0. Portable `<output-root>` commands replace workstation paths; eight existing animation generators remain source-controlled without videos or frame dumps. |
| D-025 | Treat the final full-suite warnings as documented compatibility debt, not theorem-contract failures. | The full suite passed 337 tests with zero failures/skips. Seven legacy tests return values and two calls exercise deprecated duality wrappers; canonical contract and source-law regression gates passed. |
| D-026 | Replace six repository-root/legacy SOInet result dumps with six canonical, producer-local AI capability modules. | The removed names are retained as a documented intent map: color and sound classification, bidirectional color and sound classification, and color and sound cloning. Each canonical module owns typed implementation plus `test/` and `simulation/` artifacts. Legacy entry points delegate without import-time directory creation. Capability evidence remains numerical-candidate support and does not upgrade theorem inventory rows. |
| D-027 | Supersede D-022 and remove the complete historical `tne_concepts` tree plus every pre-implementation `framework` directory. | Repository-owner direction identifies them as obsolete plans/duplicates. Canonical QENN, PGQENN, SOInets, Flowpoint, DFI, Elastic-pi, and closure implementations remain under `equations`; `.gitignore` rejects future `framework` directories. |
| D-028 | Relocate cross-cutting runtime infrastructure out of the `equations` root. | Typed theorem contracts now live under `the_nothingness_effect._runtime/theorem_complex_runtime`; shared artifact I/O lives under `the_nothingness_effect._runtime/artifacts`; domain artifact runtimes live under fluctuation/elastic and gravitational/cosmological/quantum packages; aggregate animation reports live in `docs/data` and their runner in `tools`. |
| D-029 | Require QENN, PGQENN, and SOInets to demonstrate all six observable AI output groups in both architecture-local test and simulation suites. | Each suite imports its own architecture, records native residuals, and invokes color/sound classification, bidirectional classification, and cloning producers. Artifacts and manifests remain beneath the architecture's own producer folder. |
| D-030 | Rebuild DTQC visual evidence from current canonical contracts and move Hawking theoretical benchmarks beneath black-hole dynamics. | The five legacy DTQC PNG concepts were inspected and deleted; new contour, FFT, wavelet, DFI, Elastic-pi, and phase-clock outputs are regenerated under `dtqc/test` and `dtqc/simulation`. Hawking test/simulation benchmarks now live under `black_hole_dynamics/hawking` without duplicating shared formulas. |
| D-031 | Record the post-restructure full-suite result independently of the historical D-025 checkpoint. | After removing eight tests contained only in deleted legacy trees and adding canonical producer/layout coverage, the repository passes 343 tests with zero failures/skips and nine documented legacy warnings in 293.99 seconds. |
| D-032 | Never infer a passive B/C implementation from appendix identifiers containing `carrier`, a product domain, or `\\prod`. | Repository-owner correction exposed an invalid lexical heuristic. All 147 B/C rows are evaluated semantically through their derived operator, residual/invariant, complete source dependencies, source-removal checks, and spatial closure obligations. A carrier is a valid typed domain; only an implementation that merely returns or stores its sources without a derived law is a violation. The regenerated inventory records zero lexical carrier blockers and keeps genuine dependency gaps separate as `partial`. |
